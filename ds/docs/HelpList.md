---
category: Panels
---

Stacked list of bordered help entries inside a `Modal`. Children are plain `<li>` items; use `<b>` for the lead phrase and `Kbd` for shortcuts.

```tsx
<HelpList>
  <li><b>Search</b> — <Kbd>Ctrl K</Kbd> opens the session switcher.</li>
</HelpList>
```
