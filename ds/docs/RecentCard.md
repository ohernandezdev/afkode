---
category: Empty state
---

Recent-project card: bold project `name` over its dimmed truncated `path`; `selected` gives the accent border. Compose inside `Recents`.

```tsx
<RecentCard selected name="afkode" path="C:\\Projects\\afkode" />
```
