---
category: Panels
---

Floating terminal search bar (top-right of the terminal area). Renders a magnifier, an input, and trailing icon Buttons (children replace the default close). Positions absolutely — needs a positioned ancestor.

```tsx
<SearchBar placeholder="Search scrollback" defaultValue="error" />
```
