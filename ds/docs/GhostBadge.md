---
category: Status
---

Floating accent pill announcing ghost (click-through) mode. Positions absolutely near the top of the `Overlay` — needs a positioned ancestor.

```tsx
<GhostBadge>Ghost mode — click-through</GhostBadge>
```
