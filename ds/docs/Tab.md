---
category: Navigation
---

Session tab. The dot shows live agent state: `working` (accent), `waiting` (amber, blinking), `done` (green), `dead` (grey). `active` marks the selected tab; `closable` shows the × affordance.

```tsx
<Tab active state="working" closable>afkode · claude</Tab>
```
