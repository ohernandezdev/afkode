---
category: Empty state
---

Agent launcher button with its identity dot: `claude` (accent), `oc` (green), `cx` (light grey), `ps` (blue). `missing` dims it with a download hint. Must sit inside `Launchers`.

```tsx
<LauncherButton agent="claude">Claude Code</LauncherButton>
<LauncherButton agent="cx" missing>Codex</LauncherButton>
```
